{
  "dependencies": {
    "@google/maps": "^1.1.3",
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1",
    "fastify": "^5.4.0",
    "uuid": "^9.0.0"
  },
  "description": "DEWI World Fastify backend API",
  "devDependencies": {
    "@types/google__maps": "^0.5.20",
    "@types/node": "^24.0.4",
    "@types/uuid": "^9.0.0",
    "cross-env": "^7.0.3",
    "fastify-tsconfig": "^3.0.0",
    "nodemon": "^3.1.10",
    "prettier": "^3.6.2",
    "rimraf": "^5.0.7",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.0"
  },
  "name": "@dewi/api",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "clean": "rimraf dist",
    "dev": "cross-env NODE_ENV=development nodemon",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "echo 'No linting configured for server yet'",
    "start": "node dist/index.js",
    "watch": "tsc --watch --preserveWatchOutput --pretty"
  },
  "version": "1.0.0"
}
